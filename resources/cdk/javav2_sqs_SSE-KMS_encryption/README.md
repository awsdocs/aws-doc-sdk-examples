When the CDK is ready for use, run the following command to output the template to the service example that uses it.

```aiignore
cdk synth --yaml >  ../../../javav2/example_code/sqs/src/main/resources/template.yaml 
```
