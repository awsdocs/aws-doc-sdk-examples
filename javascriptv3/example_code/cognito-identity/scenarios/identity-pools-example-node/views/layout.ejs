<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Cognito Identity Pools Demo</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }
    .container {
      border: 1px solid #ddd;
      padding: 20px;
      border-radius: 5px;
      margin-bottom: 20px;
    }
    h1, h2, h3 {
      color: #0066cc;
    }
    .provider-buttons {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin: 20px 0;
    }
    .provider-button {
      display: inline-block;
      padding: 10px 15px;
      border-radius: 4px;
      text-decoration: none;
      color: white;
      font-weight: bold;
    }
    .facebook { background-color: #3b5998; }
    .twitter { background-color: #1da1f2; }
    .amazon { background-color: #ff9900; }
    .apple { background-color: #000000; }
    .google { background-color: #4285f4; }
    pre {
      background-color: #f5f5f5;
      padding: 15px;
      border-radius: 4px;
      overflow-x: auto;
    }
    .nav {
      display: flex;
      justify-content: space-between;
      margin-bottom: 20px;
      padding-bottom: 10px;
      border-bottom: 1px solid #eee;
    }
    .nav a {
      color: #0066cc;
      text-decoration: none;
    }
    .nav a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="nav">
    <div>
      <a href="/">Home</a>
    </div>
    <div>
      <% if (typeof user !== 'undefined' && user) { %>
        <span>Logged in as <%= user.displayName %></span> | 
        <a href="/auth/logout">Logout</a>
      <% } %>
    </div>
  </div>
  
  <%- body %>
  
  <footer style="margin-top: 40px; text-align: center; color: #666; font-size: 0.8em;">
    <p>Amazon Cognito Identity Pools Demo</p>
  </footer>
</body>
</html>
