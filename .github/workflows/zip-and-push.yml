name: Zip repository and put on S3
on: [workflow_dispatch]

jobs:
  zip-n-push:
    name: Zip and Push
    runs-on: ubuntu-latest
    steps:
      - name: Zip Folder
        run: |
          cd ../ # I added this
          rm -rf aws-doc-sdk-examples.zip
          zip -r aws-doc-sdk-examples.zip aws-doc-sdk-examples -x ".git/*" ".github/*"